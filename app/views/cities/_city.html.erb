<%= form_for([@state, @city]) do |f| %>   
    <%= f.label :name %> <br />
    <%= f.text_field :name %> <br />
    <%= f.label :population %> <br />
    <%= f.text_field :population %> <br />      
    <%= f.submit %>
<% end %>